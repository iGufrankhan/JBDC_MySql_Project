public class Main {

    public static void main(String[] args) {

        // CRUD Operations --> Create, Read, Update, Delete, Drop, Truncate

        // Step 1: Create the database
       //  Create_database.Create_data();

        // Step 2: Create the table inside the created database
        // Create_operation.create();

        // Step 3: Insert data into the table
        // Insert_operation.insert("paray", 29, 90.9);

        // Step 4: Read operation
        // Read_operation.readLimit(3);  // Read top 3 records
        // Read_operation.read_cond("id", 1);  // Read by condition (e.g., where id = 1)

        // Step 5: Update operation
        // Update_operation.Update();  // Or update a specific record
        // Update_operation.Update(2, 100);

        // Step 6: Delete operations
        // Delete_operation.Delete_Allrow();  // Delete all rows
        // Delete_operation.Delete_Ind_row(2);  // Delete specific row by ID

        // Step 7: Drop the table
      //  DropTrunc_Table.dropTable();  // Drop the table from the database

        // Step 8: Truncate the table
        // Drop_or_Truncate.truncateTable();  // Remove all rows from the table, keeping its structure

        // step 9:Drop the Database
       // Drop_Database.dropDatabase();


    }
}
